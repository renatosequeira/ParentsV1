@model IEnumerable<Parents.Domain.ParentalManagement.Helpers.ParentalType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ParentalTypeDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ParentalTypeDescription)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ParentalTypeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ParentalTypeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ParentalTypeId })
        </td>
    </tr>
}

</table>
