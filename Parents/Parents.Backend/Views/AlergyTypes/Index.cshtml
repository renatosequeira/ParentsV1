@model IEnumerable<Parents.Domain.HealthManagement.Categories.AlergyType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AlergyTypeDescriptiom)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AlergyTypeCharacteristics)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AlergyTypeDescriptiom)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AlergyTypeCharacteristics)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AlergyTypeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.AlergyTypeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AlergyTypeId })
        </td>
    </tr>
}

</table>
