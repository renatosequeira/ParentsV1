@model IEnumerable<Parents.Domain.Sistema.WeightUnits>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.WeightUnitDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.WeightUnitDescription)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.WeightUnitId }) |
            @Html.ActionLink("Details", "Details", new { id=item.WeightUnitId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.WeightUnitId })
        </td>
    </tr>
}

</table>
