@model IEnumerable<Parents.Domain.SchoolManagement.Helpers.ExamFamily>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ExamFamilyDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ExamFamilyDescription)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ExamFamilyId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ExamFamilyId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ExamFamilyId })
        </td>
    </tr>
}

</table>
