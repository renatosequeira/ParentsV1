@model IEnumerable<Parents.Domain.HealthManagement.Categories.UrgencyCategory>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.UrgencyCategoryDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.UrgencyCategoryDescription)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.UrgencyCategoryId }) |
            @Html.ActionLink("Details", "Details", new { id=item.UrgencyCategoryId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.UrgencyCategoryId })
        </td>
    </tr>
}

</table>
