@model IEnumerable<Parents.Domain.TasksManagement.Helpers.TaskFamily>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TaskFamilyDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TaskFamilyDescription)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TaskFamilyId }) |
            @Html.ActionLink("Details", "Details", new { id=item.TaskFamilyId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TaskFamilyId })
        </td>
    </tr>
}

</table>
