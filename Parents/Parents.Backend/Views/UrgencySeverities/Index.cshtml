@model IEnumerable<Parents.Domain.HealthManagement.Categories.UrgencySeverity>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.UrgencySeverityDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.UrgencySeverityDescription)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.UrgencySeverityId }) |
            @Html.ActionLink("Details", "Details", new { id=item.UrgencySeverityId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.UrgencySeverityId })
        </td>
    </tr>
}

</table>
