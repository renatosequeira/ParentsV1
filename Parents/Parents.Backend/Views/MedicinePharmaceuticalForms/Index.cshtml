@model IEnumerable<Parents.Domain.HealthManagement.Categories.MedicinePharmaceuticalForm>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MedicinePharmaceuticalFormDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MedicinePharmaceuticalFormDescription)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MedicinePharmaceuticalFormId }) |
            @Html.ActionLink("Details", "Details", new { id=item.MedicinePharmaceuticalFormId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MedicinePharmaceuticalFormId })
        </td>
    </tr>
}

</table>
