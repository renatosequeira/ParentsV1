@model IEnumerable<Parents.Domain.ActivitiesManagement.Helpers.ActivityInstitutionType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ActivityInstitutionTypeDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ActivityInstitutionTypeDescription)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ActivityInstitutionTypeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ActivityInstitutionTypeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ActivityInstitutionTypeId })
        </td>
    </tr>
}

</table>
