@model IEnumerable<Parents.Domain.ActivitiesManagement.Helpers.ActivityPeriodicity>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ActivityPeriodicityDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ActivityPeriodicityDescription)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ActivityPeriodicityId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ActivityPeriodicityId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ActivityPeriodicityId })
        </td>
    </tr>
}

</table>
