@model IEnumerable<Parents.Domain.HealthManagement.Categories.PhysicalCharacteristicType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PhysicalCharacteristicTypeDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PhysicalCharacteristicTypeDescription)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PhysicalCharacteristicTypeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.PhysicalCharacteristicTypeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PhysicalCharacteristicTypeId })
        </td>
    </tr>
}

</table>
