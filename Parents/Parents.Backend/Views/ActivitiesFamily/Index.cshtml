@model IEnumerable<Parents.Domain.ActivitiesManagement.Helpers.ActivityFamily>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ActivityFamilyDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ActivityFamilyDescription)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ActivityFamilyId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ActivityFamilyId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ActivityFamilyId })
        </td>
    </tr>
}

</table>
